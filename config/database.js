
const database = {
  local: {
    adapter: 'mysql', //adapter (currently only mysql is supported.)
    database: 'news',//database Name
    host: 'localhost',
    user: 'root',
    password: '',
    port: '3306'
  }
}
export default database;